# TDD Plan

Generated by the supervisor-owned dual-agent workflow driver.

## Intent

Review the implementation of supervisor-owned dual-agent workflow lifecycle: harness health matrix, workflow state tables, run_dual_agent_workflow MCP tool, max-round enforcement, mandatory artifact export, claim verification, milestone events, and resume prompt. Inspect the current git diff and tests. Accept only if the lifecycle is supervisor-owned rather than relying on Codex prompt-following.
