# Issues

## Issue 1: Add Interaction Projection

Generate `interactions.md` from the same supervisor ledger events used by `transcript.md`, but present it as a dialogue-oriented summary.

## Issue 2: Preserve Raw Transcript

Keep `transcript.md` as the raw ledger detail. `interactions.md` should not replace it.

## Issue 3: Update Skill Contract

Document that operators should use `interactions.md` as the primary human-readable interaction record.
