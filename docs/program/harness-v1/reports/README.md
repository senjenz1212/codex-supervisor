# Harness v1 Reports

Store program-level readouts here after their evidence bundles pass
`ClaimGate`. A report must identify its claim IDs, maximum authorized level,
evidence-bundle hash, and any blocked higher-level claims.

Historical reports stay at their original paths and are referenced by
`legacy-map.yaml`.
