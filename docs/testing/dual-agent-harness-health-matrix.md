# Dual-Agent Harness Health Matrix

Purpose: evaluate the reliability harness, not the model vibes.

Legend:

- green: covered by code, tests, or live evidence
- yellow: partially covered; operator discipline or manual evidence still matters
- red: known missing implementation
- unknown: not probed yet

| Workflow | E execution | T tools | C context | L lifecycle | O observability | V verification | G governance |
|---|---|---|---|---|---|---|---|
| code-only implementation | green: Claude `/lead` via `start_dual_agent_gate` ([coverage](dual-agent-slice0-coverage-index.md)) | green: Codex MCP tools exposed ([public boundary](public-boundaries.md)) | green: handoff packet + planning checksums + PRD/TDD skill receipts ([coverage](dual-agent-slice0-coverage-index.md)) | green: `run_dual_agent_workflow` owns gate order | green: SQLite ledger + transcript export + trace envelope ([coverage](dual-agent-slice0-coverage-index.md)) | green: P1/P2/P3 + tests evidence + claim receipts | green: gate prereqs, skill receipts, artifact preflight, and failure taxonomy |
| user-facing UI implementation | green: same `/lead` execution | green: same MCP tools plus screenshots payload | yellow: screenshot metadata supplied by Codex/operator | green: workflow driver reaches outcome review only after prior gates | green: `screenshots.md`, `interactions.md`, `transcript.md` | yellow: evidence contract enforced; live UI capture quality is still operator/tool dependent | green: strict `user_facing=True` blocks without visual validation |
| Vela/Slack/Calendar live-provider change | green: same `/lead` execution | green: same MCP tools plus screenshots payload | yellow: screenshot metadata supplied by Codex/operator | green: workflow auto-requires visual evidence for Vela, Slack, Calendar, screenshots, and live-provider terms | green: `visual_evidence_policy`, `screenshots.md`, `interactions.md`, `transcript.md` | yellow: code tests plus Computer Use/Browser evidence required before final acceptance | green: omitted `user_facing` no longer bypasses the visual evidence gate |
| bug fix | green: code workflow path | green: MCP tool path | green: PRD/TDD/implementation source artifacts required | green: ordered gates | green: ledger + artifacts | green: tests and changed_files claims checked | green: missing tests trigger verification scrutiny |
| PR review | yellow: review can run as artifact-only gate, not a first-class workflow preset | green: gate tools and transcript reader | green: review artifacts can be supplied as planning artifacts | yellow: workflow driver can sequence review gates but PR-specific lifecycle is not specialized | green: transcript/artifact export | yellow: PR checks/remote receipts are claim-verifier inputs, not fetched automatically | yellow: unsupported push/merge claims block unless verified |
| artifact-only review | green: no code execution required beyond `/lead` review | green: MCP gate tools | green: source artifacts mandatory | yellow: current driver still runs the full lifecycle; no first-class stop-before-execution preset yet | green: exported Markdown | green: artifact preflight | green: immutable planning artifact checksums |
| continue existing run | yellow: resume prompt generated; rerun is explicit | green: `read_dual_agent_workflow_resume_prompt` | green: state tables hold current gate/status | yellow: driver records state, but continuation re-enters by rerunning the workflow | green: workflow tables + ledger | yellow: replay fixtures cover state; live continuation remains manual | green: blocked runs expose next safe action |
| Telegram-directed run | yellow: milestones are emitted when notifier exists | green: Telegram notifier path exists | yellow: Telegram control context is separate from Desktop flow | yellow: workflow milestones exist; Telegram initiation is not first-class here | green: actions/events record milestones | yellow: live phone smoke deferred | yellow: approvals exist for deadlock/validation, not full workflow commands |
| Vela delegate | yellow: generic workflow driver can run it through Claude `/lead` | yellow: supervisor tools exist outside this driver | yellow: depends on Vela artifacts | yellow: live-surface evidence policy exists, but no full Vela-specific preset yet | yellow: supervisor ledger can observe | yellow: Vela live-surface screenshot replay added; full provider E2E still manual | yellow: existing supervisor governance applies outside this driver |
| Claude delegate | green: Claude `/lead` is the worker surface | green: Claude CLI invocation is wrapped | green: handoff packet is typed | green: driver owns lifecycle around Claude execution | green: transcript/outcome capture | green: P2/P3 validation | green: no accepted final state without outcome review |
| Cursor reviewer | green: local Cursor SDK review boundary has a green live probe ([live evidence](dual-agent-slice0-live-evidence.md)) | yellow: `cursor-sdk` optional dependency; no Cursor MCP server exposed by default | green: receives the same source artifacts, Claude outcome, and receipts at configured gates | green: workflow exposes `cursor_review_profile` and `cursor_review_gates`; default reviews final gate, rigorous reviews TDD/plan/final, vague reviews PRD/TDD/plan/final | green: `tri_agent_cursor_review` events are included in transcript output | green: live Cursor accept fixture and live tri-agent failure fixture are replay-tested | green: Cursor is reviewer-only; non-accept blocks the selected gate, and supervisor P11 can still block accepted model outcomes |
| Codex delegate | yellow: `start_codex_session` exists, but workflow execution delegates to Claude `/lead` | green: Codex-facing MCP tools | yellow: Codex PRD/TDD skills remain operator/model-driven before driver invocation | green: driver owns post-invocation lifecycle | green: ledger and artifacts | yellow: Codex Desktop live GUI probe is documented; CLI E2E is proven | green: supervisor remains acceptance boundary |

Current bottleneck: lifecycle is now implemented for the generic dual-agent path through `run_dual_agent_workflow`, and Cursor is live-proven as an optional third reviewer. Vela/Slack/Calendar live-provider tasks now auto-require visual evidence before outcome review, but full live-provider E2E runs still depend on real credentials, Computer Use evidence, and operator-visible receipts. Workflow-specific presets such as PR review and Telegram-directed run still need dedicated policies before they should be called green.
